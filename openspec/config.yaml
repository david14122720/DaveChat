# openspec/config.yaml — DaveChat SDD Configuration
# Auto-generated by sdd-init on 2026-06-29
schema: spec-driven

context: |
  Project: DaveChat — a real-time messaging and video call platform similar to WhatsApp Web.

  Frontend: React 18 + Vite 5 + Tailwind CSS 3 (JavaScript/JSX, not TypeScript yet).
  Backend: Go 1.25 signaling server with pgx for PostgreSQL. Soon-to-be API server.
  Database: Supabase/PostgreSQL (current), being migrated to MariaDB via Docker.
  Auth: Supabase Auth (current), being migrated to JWT-based custom auth.
  Real-time: WebSocket-based signaling (server/internal/websocket/, server/internal/signaling/).
  Infrastructure: Docker (no Dockerfile yet), Supabase cloud.

  Testing: No test infrastructure detected. No test files, no test scripts, no CI config.

rules:
  proposal:
    - Include rollback plan for risky changes (especially auth/db migrations)
    - Call out migration impact for Supabase→MariaDB and Supabase Auth→JWT
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns (JSX for frontend, Go std patterns for backend)
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: "cd client && npm run build && cd ../server && go build ./..."
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false
  reason: "No test runner, no test files, no test scripts detected in either frontend or backend"
  runner: null
  layers:
    unit: null
    integration: null
    e2e: null
  coverage: null
  quality:
    linter: null
    type_checker: null
    formatter: null
